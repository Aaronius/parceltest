{
  "name": "parceltest",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "rm -rf dist && parcel src/view/**/*.html --out-dir dist/view --public-url ."
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "parcel": "^1.12.3"
  }
}
